# Claude Code Instructions

The canonical project instructions are in [AGENTS.md](AGENTS.md). Read and
follow that file for repository structure, editing boundaries, generated files,
validation commands and deployment conventions.

Do not duplicate project rules here; update `AGENTS.md` instead.
